---
ukiryu_schema: '1.0'
name: awk
version: '1.36.1'
interface: awk
implementation: busybox

profiles:
  - name: default
    platforms: [macos, linux]
    shells: [unix]
    commands:
      - name: print
        execute: ["awk", "{print}"]
        description: "Print lines"
