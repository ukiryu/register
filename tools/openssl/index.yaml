---
ukiryu_schema: '1.0'
name: openssl
interface: openssl
implementations:
- name: default
  display_name: Openssl
  detection:
    pattern: openssl ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '3.4'
    file: 3.4.yaml
  default: 3.4.yaml
