---
ukiryu_schema: '1.0'
name: wget
version: '1.24'
interface: wget
implementation: gnu

profiles:
  - name: default
    platforms: [macos, linux]
    shells: [unix]
    commands:
      - name: download
        execute: ["wget"]
        description: "Download file"
