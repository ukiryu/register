---
ukiryu_schema: '1.0'
name: cut
interface: cut
implementations:
- name: default
  display_name: Cut
  detection:
    pattern: cut ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '0.1'
    file: 0.1.yaml
  - equals: generic
    file: generic.yaml
  default: generic.yaml
