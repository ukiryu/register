---
ukiryu_schema: '1.0'
name: tail
interface: tail
implementations:
- name: default
  display_name: Tail
  detection:
    pattern: tail ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '0.1'
    file: 0.1.yaml
  - equals: generic
    file: generic.yaml
  default: generic.yaml
