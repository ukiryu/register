---
ukiryu_schema: '1.0'
name: ping
version: '1.0'
interface: ping
implementation: bsd

profiles:
  - name: default
    platforms: [macos, linux]
    shells: [unix]
    commands:
      - name: ping
        execute: ["ping", "-c", "1"]
        description: "Ping host"
