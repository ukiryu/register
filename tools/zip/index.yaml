---
ukiryu_schema: '1.0'
name: zip
interface: zip
implementations:
- name: default
  display_name: Zip
  detection:
    pattern: zip ([\d.]+)
  version_scheme: semantic
  versions:
  - equals: '3.0'
    file: 3.0.yaml
  default: 3.0.yaml
